- 任务1
  - 参考demo/edit_stock.html修改stock 编辑界面
  - 点击stock的编辑按钮，进入编辑界面，显示已有的json，编辑按钮，更新数据库里的json。
- 任务2
  - 参考demo/create_stock.html修改stock录入界面
  - 现在不需要单独录入ticker，ticker也在json里面
- 任务3
  - 参考demo/report.tsx修改报告页
  - 新增了报告的版本列表
  - 创新新版本
  - 编辑已有版本
- 任务4
  - 参考demo/watchlist.tsx修改模块4
  - watchlist 列表页，左边是tradingview的chart，右边是watchlist列表。
  - 点击watchlist可以显示stock。
  - 点击stock，左边显示对应的chart。
  - 可以删除watchlist。
  - 可以删除watchlist的stock。当stock都删除时，watchlist也被删除。
  - 右键 watchlist 标题 → 弹出菜单（Add Symbol / Export CSV / Delete Watchlist）
  - 右键 stock → 不弹出任何菜单
  - stock 行右侧仍保留 ✕ 按钮用于删除
  - 可以拖动调整watchlist，stock的位置。并保留调整后的位置。
- 任务5
  - 模块2的左上角标题：模块2: stock module 改成 Stock
  - table中每个stock的编辑按钮改成图标
  - Create Record弹框的标题改成 Create ，标题四周的margin参考demo
  - Create Record弹框的Required: `ticker：删除`
  - Create Record弹框的json默认时空白
  - Edit Record弹框的标题改成 Create ，标题四周的margin参考demo
  - Edit Record弹框的Required: `ticker：删除`
  - `watchlist右边的列表头部的`**Watchlists，和**Right-click a watchlist to manage调整下间距
  - `watchlist左边的`**模块4: watchlist module删掉**
  - `watchlist左边的`Chart:xxx**删掉**
  - Add Symbol (Ticker)dialog改成和Create Record弹框一样，页面居中显示
- 任务6
  - Add Symbol弹框的标题改成 Add Symbol ，标题四周的margin参考demo,create 按钮改成add 
  - tradingview widget外层的容器删掉
  - Right-click a watchlist to manage的上下间距太宽了，对齐demo
  - 项目里的字体也对齐demo修改
  - watchlist的stock的选择/非选择状态的css style也对齐demo
- 任务7
  - watchlist的展开的时候，右键也弹出菜单
  - 报告页的排版，css对齐demo

